package com.soprasteria.clinic.appointment.entity;

/**
 * Represents the different roles available in the clinic appointment system.
 * These roles define the access levels and permissions for different users.
 */

public enum RoleEnum {
    ADMIN,
    DOCTOR,
    PATIENT
}
